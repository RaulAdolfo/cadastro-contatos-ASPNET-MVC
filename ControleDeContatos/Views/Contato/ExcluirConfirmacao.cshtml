@model ContatoModel
@{
    ViewData["Title"] = "Confirmar exclusão de contato";
}

<div class="text-center">
    <h3>Realmente deseja apagar o contato (@Model.Nome)?</h3>
</div>

<a class="btn btn-danger d-block w-100 mb-3" asp-controller="Contato" asp-action="Excluir" asp-route-id="@Model.Id" role="button">Excluir</a>
<a class="btn btn-secondary d-block w-100 mb-3" asp-controller="Contato" asp-action="Index" role="button">Voltar</a>
